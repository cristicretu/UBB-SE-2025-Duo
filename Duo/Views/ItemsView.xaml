<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Duo.Views.ItemsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Item Management" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

        <!-- Add new item -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="NewItemTextBox" PlaceholderText="Enter a new item" Width="300" 
                     Text="{x:Bind ViewModel.NewItemName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Button x:Name="AddButton" Content="Add" Margin="10,0,0,0" 
                    Command="{x:Bind ViewModel.AddItemCommand}"/>
        </StackPanel>

        <!-- Items list -->
        <ListView Grid.Row="2" x:Name="ItemsListView" 
                  ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedItem, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Id}" VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1" Text="{Binding Name, Mode=TwoWay}" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Update and Delete buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="UpdateButton" Content="Update Selected" Margin="0,0,10,0" 
                    Command="{x:Bind ViewModel.UpdateItemCommand}"/>
            <Button x:Name="DeleteButton" Content="Delete Selected" 
                    Command="{x:Bind ViewModel.DeleteItemCommand}"/>
        </StackPanel>
    </Grid>
</UserControl> 